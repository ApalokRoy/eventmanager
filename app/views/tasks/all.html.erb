<% provide(:title, 'Tasks Page') %>
<%= link_to 'Back', root_path, class: "btn btn-lg btn-primary", id: "button1" %>
<br>
<h1> All Tasks </h1>

<%= react_component 'Tasks', { tasks: @tasks.as_json( :include=> { :event => { only: [:name] }, :user => { only: [:name] } }) } %>
